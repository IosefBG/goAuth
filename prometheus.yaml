global:
  scrape_interval:     15s # How frequently to scrape targets
  evaluation_interval: 15s # How frequently to evaluate rules

scrape_configs:
  - job_name: 'go-app'
    static_configs:
      - targets: ['localhost:8080']  # Replace with the address where your Go application is running
